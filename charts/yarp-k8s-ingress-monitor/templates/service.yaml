apiVersion: v1
kind: Service
metadata:
  name: {{ include "yarp-k8s-ingress-monitor.fullname" . }}
  labels:
    {{- include "yarp-k8s-ingress-monitor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "yarp-k8s-ingress-monitor.selectorLabels" . | nindent 4 }}
